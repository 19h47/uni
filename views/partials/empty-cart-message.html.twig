{#
 # Empty Cart Message
 #}
{{-
	include(
		'woocommerce/notices/notice.html.twig',
		{
			notices: [{
				classes: [ 'cart-empty' ],
				notice: __( 'Your cart is currently empty.', 'uni' ) | apply_filters( 'wc_empty_cart_message' )
			}]
		}
	)
-}}
