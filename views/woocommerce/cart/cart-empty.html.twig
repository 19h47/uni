{#  #}
{% block content %}

	{% do action( 'woocommerce_cart_is_empty' ) %}

	{% if page_id > 0 %}
		<p class="return-to-shop">
			<a class="button wc-backward" href="{{ url }}">
				{{- pll__( 'Return to shop' ) -}}
			</a>
		</p>
	{% endif %}

{% endblock %}
