{#
 # Loop start
 #
 # @package UNI
 #}
<ul
	class="{{
		[
			'mx-n20px',
			'mx-md-0',
			'my-md-n20px',
			'list-style-type-none',
			'md-flex',
			'products',
			"columns-#{ columns | default( 3 ) }"
		] | merge( classes | default( [] ) ) | join( ' ' ) | trim
	}}"
>
