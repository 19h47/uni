{#
 # Notice
 #}

{% if notices is iterable and notices is not empty %}
	{% for notice in notices %}
		<div
			class="{{
				[
					'shadow-uni',
					'p-12px',
					'border',
					'border-cyan-200',
					'rounded',
					'bg-cyan-100',
					'text-cyan-800',
				] | merge( notice.classes | default( [] ) ) | join( ' ' ) | trim
			}}"
			{{ fn( 'wc_get_notice_data_attr', notice ) }}
		>
			{{-  notice.notice }}
		</div>
	{% endfor %}
{% endif %}
