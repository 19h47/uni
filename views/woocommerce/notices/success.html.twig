{#
 #
 #}
{% if notices is iterable and notices is not empty %}

	<div class="flex flex-column gap-y-12px">
		{% for notice in notices %}
			<div class="{{ [
				'shadow-uni',
				'p-12px',
				'border',
				'rounded',
				'text-green-800',
				'bg-green-100',
				'border-green-200'
			] | join( ' ' ) | trim }}"
			{{ fn( 'wc_get_notice_data_attr', notice ) }}
			role="alert"
		>
				{{- notice.notice -}}
			</div>
		{% endfor %}
	</div>

{% endif %}
