{#
 # Payment method
 #
 #}
<li class="wc_payment_method payment_method_{{ gateway.id }}">
	<input
		id="payment_method_{{ gateway.id }}"
		type="radio" class="input-radio"
		name="payment_method" value="{{ gateway.id }}"
		{{ fn( 'checked', gateway.chosen, true ) -}}
		{{ gateway.order_button_text ? "data-order_button_text=\"#{ gateway.order_button_text }\"" | raw }}
	/>

	<label for="payment_method_{{ gateway.id }}">
		{{- gateway.get_title() }}&nbsp;{{ gateway.get_icon() -}}
	</label>

	{% if gateway.has_fields() or gateway.get_description() %}
		<div class="payment_box payment_method_{{ gateway.id }}"{{ gateway.chosen ?: " style=\"display:none;\"" }}>
			{{- gateway.payment_fields() -}}
		</div>
	{% endif %}
</li>
